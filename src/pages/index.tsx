import React, { FC } from "react";

import { Layout } from "../components";

const IndexPage: FC = () => {
    return (
        <Layout>
            <div>Index!</div>
        </Layout>
    );
};

export default IndexPage;
